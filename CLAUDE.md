# CarAppStore — 项目级规则

> 全局约束见 `C:\Users\XZQ\.claude\CLAUDE.md`（Android 全局开发规范）。
> 本文档仅包含 CarAppStore 项目特有的约束，不与全局规范重复。
> 优先级：用户要求 > 项目级 CLAUDE.md > 全局 CLAUDE.md > 既有代码风格。
> 项目背景与进度见 `README.md`，详细架构见 `docs/`。
> 若文档与代码现状冲突，以代码现状为准，并明确指出差异。

---

## 0. 接手流程

进入仓库后先读：`README.md` → `docs/21-当前项目状态与接手指南.md`
开始编码前必须先总结当前进度（阶段、已完成能力、风险、下一步）。
需要确认落地状态时：`git status --short` + 最近关键提交 + 对应模块源码。

---

## 1. 业务模块边界

以下 7 个业务模块边界不可破坏：

1. **下载** — 应用下载引擎
2. **安装** — 安装会话管理
3. **升级** — 版本升级策略
4. **应用管理** — 已安装应用生命周期
5. **状态中心** — 全局状态聚合与分发
6. **策略中心** — 业务策略配置与决策
7. **Repository** — 数据编排层

跨模块调用必须通过公开接口完成，禁止直接访问其他模块内部实现。

---

## 2. 项目特有约束

- `app` 模块只做壳层装配和系统桥接，禁止重新业务化。
- 依赖注入固定 `AppContainer` 手动装配，禁止引入 `Hilt` / `Dagger`。
- 页面结构固定 `Activity + FragmentManager`，禁止引入 `Navigation`。

---

*最后更新：2026-05-09*
