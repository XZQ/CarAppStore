# 21. 当前项目状态与接手指南

> 最后更新：2026-07-31 | 最新提交请以 `git log --oneline -5` 为准
> 产品定位：CarAppStore 是跨平台应用分发与管理 App；本仓库是 Android 客户端，详细边界见 `docs/00-产品定位与平台边界.md`。

## 1. 一句话阶段判断

**跨平台产品的 Android 主链路已通，真实能力 R1-R6 + P1/P2 硬化 + P3 代码重构 + P4/P5 发布边界完成；通用平台与可选车载策略已解耦，`supportedPlatforms/currentPlatform` 已进入目录、展示和下载/安装/升级执行边界，compileSdk/targetSdk 已升级到 36。2026-07-31 的 JDK 17 全量单测、编译、Lint、Debug/Release 构建均通过；Android 设备矩阵、生产服务、签名和观测验收仍未完成。**

当前换机接手总览见 `docs/29-换机接手与当前进度总览.md`。平台策略解耦、API 36 和平台能力模型均已逐项提交并推送；实际提交基线以本机 `git log --oneline -5` 为准。

## 2. 已完成里程碑

| 里程碑 | 内容 | 关键文件 |
|--------|------|---------|
| **R1** 真实下载 | HTTP下载、Range分段、校验、断点续传、ETag/Last-Modified | `RealFileDownloader.kt` |
| **R2** 真实安装 | PackageInstaller.Session、用户确认流、Session持久化 | `RealPackageInstaller.kt` |
| **D** 统一数据层 | LocalStoreFacade、JsonBackedLocalStoreFacade、实体映射 | `data/local/` |
| **R3** 执行控制 | IO中断(registerInterrupt)、网络异常处理、去重保护 | `DownloadExecutionControl.kt` |
| **R4** Repository真实化 | RealAppRepository、AppSystemDataSource接入PackageManager | `RealAppRepository.kt` |
| **R5** 数据层增强 | VersionedJsonStore: schema版本、迁移、ReentrantLock并发保护、原子写 | `VersionedJsonStore.kt` |
| **R6** 升级能力 | checkAllUpgrades、startBatchUpgrade串行批量、策略门控 | `DefaultUpgradeManager.kt` |
| **P1 基础硬化** | 商店目录 HTTP/条件请求缓存/资源回退链路、策略设置流、网络/存储实时信号、可选车载 provider 接缝、首页/搜索/详情/我的应用/任务中心状态机 | `AppRemoteDataSource.kt` / `DefaultPolicyCenter.kt` / `VehicleStateSignalProvider.kt` / `feature-*` |
| **P2 生产硬化** | 系统边界输入校验+10 个 validation 测试、下载基地址外化到 DownloadEnvironmentConfig、DownloadSourceCatalog 策略从 config 读取（不再硬编码环境判断）、数据层 Mapper 测试补齐（9 个）、XML 168+ 处尺寸全量外化到 dimens.xml、开发者工具扩展（策略信号/版本信息/缓存管理）、Android 设备回归清单与后端/平台适配器验收标准输出 | `DefaultDownloadManager.kt` / `DownloadEnvironmentConfig.kt` / `DownloadSourceCatalog.kt` / `DeveloperSettingsFragment.kt` / `dimens.xml` / `docs/25` / `docs/26` |
| **P3 代码重构** | 超长方法拆解（全部≤40行）：RealFileDownloader.download 159→30行、DefaultDownloadManager.executeDownload 191→19行、AppLocalDataSource.loadFromDisk 105→28行、PackageInstallerSessionAdapter.commitSession 65→29行；抽出 DownloadFileHelper 收敛文件操作；XML 嵌套扁平化（≤4层）；Magic numbers 提取到 companion object；新增 SegmentPlanner/DownloadSourceResolver/DownloaderText 测试 14 个 | `RealFileDownloader.kt` / `DownloadFileHelper.kt` / `DefaultDownloadManager.kt` / `AppLocalDataSource.kt` / `PackageInstallerSessionAdapter.kt` / `SegmentPlanner.kt` |
| **P4 安全边界** | 目录标识和签名字段校验、下载任务 canonical containment、APK 包名/versionCode/签名证书校验、安装后 PackageManager 事实核对 | `AppCatalogValidator.kt` / `DownloadStore.kt` / `ApkVerifier.kt` / `AndroidPackageIdentityVerifier.kt` / `RealPackageInstaller.kt` |
| **P5 发布与恢复边界** | Release 固定 PROD、release raw 安全空目录、生产地址不再回退 example、同文件跨 Store 实例共享锁、冷启动对账平台安装 Session 与 PackageManager 事实、GitHub Actions 验证与产物归档 | `DownloadEnvironmentProvider.kt` / `data/src/release/res/raw` / `VersionedJsonStore.kt` / `InstallSessionReconciler.kt` / `.github/workflows/android-ci.yml` |
| **P6 平台边界** | 通用平台不再受驻车信号误拦；目录支持 `supportedPlatforms`，客户端显式维护 `currentPlatform`；其他平台安装包在卡片、详情和业务执行入口统一禁用；所有模块升级到 compileSdk 36，App targetSdk 36 | `PlatformCapabilities.kt` / `PlatformActionResolver.kt` / `DefaultPolicyCenter.kt` / `AppContainer.kt` / `build.gradle.kts` |

## 3. 测试覆盖

### 最近全量验证：2026-07-31 共 45 份报告、246 个测试，0 失败、0 错误

| 模块 | 测试类 | 测试数 |
|------|--------|--------|
| Core | RealFileDownloaderTest | 12 |
| Core | VersionedJsonStoreTest | 6 |
| Core | DownloadStoreTest | 6 |
| Core | InstallSessionStoreTest | 2 |
| Core | InstallSessionReconcilerTest | 3 |
| Core | RealPackageInstallerTest | 7 |
| Core | ApkIdentityValidatorTest | 9 |
| Core | AndroidPackageIdentityVerifierTest | 2 |
| Core | PolicyRuntimeSignalsTest | 3 |
| Core | SegmentPlannerTest | 5 |
| Core | DownloadSourceResolverTest | 7 |
| Core | DownloaderTextTest | 2 |
| Core | VehicleSignalValueParserTest | 3 |
| Core | FileEventTrackerTest | 1 |
| Core | **EventReporterTest** ★ | 6 |
| Core | **AndroidPolicyRuntimeSignalProviderTest** ★ | 5 |
| Business | DefaultStateCenterTest | 22 |
| Business | DefaultDownloadManagerTest | 14 |
| Business | DefaultInstallManagerTest | 12 |
| Business | DefaultUpgradeManagerTest | 17 |
| Business | PolicySettingsMergerTest | 4 |
| Business | AppPrimaryActionExecutorTest | 7 |
| Business | DefaultAppManagerTest | 6 |
| Business | PlatformActionResolverTest | 2 |
| Business | **DefaultPolicyCenterTest** ★ | 7 |
| Data | JsonBackedLocalStoreFacadeTest | 1 |
| Data | AppCatalogJsonParserTest | 11 |
| Data | AppCatalogSourceTest | 5 |
| Data | AppCatalogCacheMetadataStoreTest | 2 |
| Data | AppCatalogGovernanceTest | 4 |
| Data | DownloadTaskMapperTest | 6 |
| Data | InstallSessionMapperTest | 3 |
| Data | **AppSystemDataSourceTest** ★ | 9 |
| Data | **RealAppRepositoryTest** ★ | 4 |
| Data | DownloadEnvironmentProviderTest / DownloadEnvironmentConfigTest ★ | 5 |
| Data | PlatformCapabilitiesTest | 2 |
| Feature | 7个ViewModel测试 | 23 |

★ Robolectric 测试（core/business/data 已接入，运行时使用真实 Android Context）

### 仍需设备/集成验证的边界

- 真实签名 APK 的 PackageManager archive 读取、证书轮换和安装后包可见性。
- `AppRemoteDataSource` 的真实 API、条件缓存和非法目录回退联调。

### 编码规范合规项

已合规项：MVVM 分层、无 Hilt/Navigation/!!/GlobalScope、中文 KDoc、ViewBinding、ListAdapter+DiffUtil、AppLogger、无空 catch、资源命名。壳层和首页仍保留少量用于资源限定符兼容的 optional `findViewById` 查询，不作为业务层依赖入口。完整规范以 `CLAUDE.md` 为准，Codex 特定说明见 `AGENTS.md`。

已修复项：XML 尺寸全量 @dimen/ 化、系统边界输入校验、DownloadSourceCatalog 策略从 config 读取。

P3 重构新增合规项：函数≤40行（全部达标）、Magic numbers 提取为常量、XML 嵌套≤4层。

## 4. 工程结构

```
13 modules:
  :app              — Shell: AppContainer, MainActivity
  :common           — Base classes, AppServices, UI shared
  :core             — RealFileDownloader, RealPackageInstaller, DownloadStore, VersionedJsonStore
  :data             — RealAppRepository, DataSource(remote/local/system), LocalStoreFacade
  :business         — 7 domain modules: download, install, upgrade, appmanager, state, policy
  :feature-home/detail/myapp/search/downloadmanager/installcenter/upgrade/debug
```

## 5. 关键架构决策

- **Repository**: `RealAppRepository` 代理到 3 个 DataSource，`FakeAppRepository` 仅用于测试
- **状态管理**: `DefaultStateCenter` + `StateReducer` 归约，自动推导 primaryAction 和 statusText
- **下载控制**: `DownloadExecutionControl` 支持 pause/cancel + `registerInterrupt` 连接级中断
- **数据持久化**: `VersionedJsonStore` 统一管理 JSON 文件的 schema 版本、迁移、并发锁、原子写
- **策略拦截**: `DefaultPolicyCenter` 检查 Wi‑Fi/存储，升级同时校验下载+安装策略；驻车规则只在 `vehicleInstallPolicyEnabled` 开启时参与
- **平台分流**: `supportedPlatforms/currentPlatform` 决定当前客户端是否可处理安装包，`UNSUPPORTED` 动作和业务管理器双重拒绝其他平台产物

## 6. 验证命令

Windows PowerShell:

```powershell
$env:JAVA_HOME="<your-jdk-17-path>"
.\gradlew.bat testDebugUnitTest --no-daemon
.\gradlew.bat compileDebugKotlin --no-daemon
.\gradlew.bat :app:assembleDebug --no-daemon
.\gradlew.bat lintDebug --no-daemon
.\gradlew.bat :app:assembleRelease --no-daemon
```

macOS / Linux / Git Bash:

```bash
./gradlew testDebugUnitTest --no-daemon
./gradlew compileDebugKotlin --no-daemon
./gradlew :app:assembleDebug --no-daemon
./gradlew lintDebug --no-daemon
./gradlew :app:assembleRelease --no-daemon
```

## 7. 当前边界与风险

- 远端数据已具备 HTTP/条件请求缓存/资源回退链路，但仍未接入真实 API
- `DefaultPolicyCenter` 已支持网络/存储实时信号并完成车载策略开关解耦；真实车载发行仍需接入具体 OEM 车况真值
- 安装链路需要在手机、平板和大屏设备上覆盖 Android 版本/ROM 差异；OEM/车载 ROM 只作为可选专项
- GitHub Actions hosted runner 已成功完成单测、编译、Lint、Debug/Release 构建和产物上传
- 仓库仍缺设备级自动化安装测试；当前通过的是本地 JVM/Robolectric/Lint/构建验证

## 8. 发布前外部接入事项

- **设备联调**：按 `docs/25-真机回归测试清单.md` 在 Android 设备矩阵上验证下载→安装→升级全链路。
- **远端数据接入**：按 `docs/26-后端与OEM验收标准.md` 把当前回退链路接到真实 API，并补齐 `supportedPlatforms`、鉴权、缓存治理和错误码约定。
- **生产发布配置**：注入生产目录/CDN、认证和 release 签名，生成可分发的签名产物。
- **可选车载策略接入**：仅当发行目标包含车载设备时，用真实 OEM provider 替换 `StaticVehicleStateSignalProvider` 并执行驻车/行车专项验收。
- **发布治理能力**：根据产品阶段补充灰度发布、黑白名单、埋点和安装失败归因看板。

## 9. 接手阅读顺序

1. `docs/00-产品定位与平台边界.md`
2. 本文件（你正在读的）
3. `docs/29-换机接手与当前进度总览.md`
4. `docs/01-架构总览.md`
5. `docs/03-七个业务模块详解.md`
6. `docs/09-16` 各模块架构
7. `docs/17-已完成里程碑归档.md` 各里程碑详情
8. `AGENTS.md` 编码规范

## 10. 最近 5 次提交

请直接执行：

```bash
git log --oneline -5
```

本文件不再硬编码提交列表，避免文档和仓库真实状态再次漂移。
