# 29. 换机接手与当前进度总览

> 更新时间：2026-07-11
> 用途：换电脑、重新 clone、或交给新 Agent 时，先读本文确认项目进度、验证口径和未完成事项。

## 1. 当前同步基线

- 当前主分支：`main`
- 已同步远端：`origin/main`
- APK 信任与下载路径安全实现基线：`314c30c Fix: Harden APK trust and download paths`
- 当前文档同步基线：以 `origin/main` 最新 HEAD 为准
- 当前工作区验证：2026-07-11 在 JDK 17 下通过 `testDebugUnitTest`、`lintDebug`、`:app:assembleDebug`、`:app:assembleRelease`，本地与 `origin/main` 完整哈希一致
- 权威确认方式：执行 `git status --short --branch` 和 `git log --oneline -5`，以本地拉取后的结果为准。

如果换电脑接手，先执行：

```powershell
git clone <repo-url> CarAppStore
cd CarAppStore
git status --short --branch
git log --oneline -5
```

在本机 Codex 环境里，Shell 命令需要按 `AGENTS.md` 引用的 `RTK.md` 规则使用 `rtk` 包裹。

## 2. 一句话现状

仓库内主链路、工程分层、多端 UI 壳层、本地目录回退、下载/安装/升级状态机、策略门控、事件落盘、Release 构建入口和生产配置自检都已完成；当前剩余工作主要是外部接入与真实设备验收，不是仓库内结构缺口。

## 3. 本地已完成

- 多模块结构：`app / common / core / data / business / feature-*` 已稳定。
- 页面范围：首页、搜索、详情、我的应用、下载中心、安装中心、升级中心、开发者设置均已接入。
- 多端 UI：手机、平板、车机/桌面资源限定符壳层已具备；车机/桌面含侧边导航和任务摘要。
- Repository：生产实现为 `RealAppRepository`；`FakeAppRepository` 仅用于测试。
- 远端目录链路：支持 `HTTP -> 本地缓存 -> raw 资源` 回退、ETag / Last-Modified 条件请求、鉴权头和环境变量注入。
- 下载链路：真实 HTTP、Range、分片、校验、断点续传、暂停/取消、失败恢复。
- 下载路径安全：目录标识和任务 ID 白名单、重复项拒绝、任务目录 canonical path containment。
- 安装链路：APK 包名/版本/签名预检、`PackageInstaller.Session`、用户确认页分发、安装会话持久化和安装后 PackageManager 事实核对。
- 升级链路：批量检测、串行升级、策略门控、失败重试。
- 策略链路：Wi-Fi、存储、驻车/车况策略，支持广播型 OEM 信号接入。
- 图片链路：`asset://`、`file://`、`http(s)://` 图标、头图、截图加载与文本兜底。
- 运营治理：灰度、黑白名单、隐藏/下架、回滚版本本地过滤。
- 观测入口：页面曝光、主动作、下载/安装/升级开始与结果本地事件落盘。
- Release 入口：签名环境变量与 Gradle `-P` 参数已预留，仓库不保存密钥。

## 4. 最新验证口径

当前代码基线最近一次通过：

```powershell
.\gradlew.bat testDebugUnitTest --no-daemon
.\gradlew.bat compileDebugKotlin --no-daemon
.\gradlew.bat :app:assembleDebug --no-daemon
.\gradlew.bat lintDebug --no-daemon
.\gradlew.bat :app:assembleRelease --no-daemon
```

已知产物：

- `app/build/outputs/apk/debug/app-debug.apk`
- `app/build/outputs/apk/release/app-release-unsigned.apk`

测试基线：2026-07-11 共生成 40 份 `testDebugUnitTest` 报告，220 个测试，`0` 失败、`0` 错误。

## 5. 仍未完成

这些事项需要真实外部资源或目标车机环境：

- 真实后端目录 API：正式地址、鉴权、灰度头、错误码和缓存协议。
- 真实 APK 下载源：CDN 域名、包体文件、checksum 类型和值、正数 `versionCode`、签名证书 SHA-256 摘要和灰度策略。
- OEM 车况信号：目标车机 SDK、系统广播或 provider 协议。
- 真机安装验收：不同车机 ROM 的安装确认页、回调码、权限行为和失败恢复。
- 生产签名：keystore、alias、密码由安全环境注入。
- 运营观测平台：事件上传服务、隐私字段、告警看板和失败归因。

## 6. 接手阅读顺序

1. `README.md`：项目总览、环境、构建命令。
2. `docs/21-当前项目状态与接手指南.md`：阶段判断、测试覆盖、架构决策。
3. 本文：换机接手、当前同步基线、完成/未完成边界。
4. `docs/28-商用化剩余事项.md`：外部配置入口和商用前验收顺序。
5. `docs/23-远端目录与车况信号接入约定.md`：后端与 OEM 接入字段。
6. `docs/24-后端与OEM联调执行清单.md`、`docs/25-真机回归测试清单.md`、`docs/26-后端与OEM验收标准.md`：外部联调和验收执行。
7. `docs/superpowers/specs/2026-07-11-install-trust-and-download-path-security-design.md`：APK 信任和下载路径安全设计。

## 7. 换机后的推荐动作

1. 拉取 `origin/main` 并确认工作区干净。
2. 配置 JDK/JBR 17、Android SDK 和 Gradle 环境。
3. 跑 `testDebugUnitTest`、`compileDebugKotlin`、`lintDebug`、`assembleDebug`。
4. 如果需要产物，再跑 `:app:assembleRelease`。
5. 若接真实环境，先填 `docs/28-商用化剩余事项.md` 中列出的环境变量，再按 `docs/25` 做真机回归。

## 8. 不要误判的边界

- 默认目录和默认 APK 地址仍是示例/回退值，不代表已接真实生产后端。
- Debug + LOCAL_SIM 只允许缺少版本代码/签名元数据；包名以及已提供但不匹配的版本或签名仍会严格拒绝。
- unsigned release APK 只证明 release 构建链路可生成，不等于生产签名完成。
- OEM 广播接入点已具备，但真实车机协议需要外部确认。
- 本地事件日志已具备，上传和看板仍属于外部平台工作。
