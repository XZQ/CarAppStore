# 2026-09 客户端审查修复记录

本轮基于 `dd947238` 审查后的问题清单，按项修复、验证并提交。产品定位遵循 [产品定位与平台边界](00-产品定位与平台边界.md)。本记录中的测试是 JVM / Robolectric 验证，不代表真机或生产服务验收。

| 顺序 | 项目 | 状态与验证 |
| --- | --- | --- |
| 1 | 页面刷新保留活动下载、安装、升级状态及失败原因 | 已完成。business Debug / Release 各 118 项测试通过；新增刷新活动阶段与失败状态回归。 |
| 2 | APK 失效后的重新下载与持久化任务一致性 | 已完成。business 两变体各 120 项通过；覆盖失效记录、缺失产物和重新下载后的暂停/恢复/安装。 |
| 3 | 安装异常业务边界及取消传播 | 待完成 |
| 4 | 外部卸载对账与打开失败反馈 | 待完成 |
| 5 | 清理安装失败后恢复可操作的 APK 状态 | 待完成 |
| 6 | 安装成功后清理失败的缓存可追踪与重试 | 待完成 |
| 7 | CI Release 单测、Lint、报告与文档基线 | 待完成 |
| 8 | 搜索关键词及分类的进程恢复 | 待完成 |
| 9 | 运行时短期鉴权边界 | 待完成；真实服务对接依赖后端认证契约 |
| 10 | Lint 可执行改善 | 待完成 |
| 11 | 真机与生产验收 | 等待可用设备及服务资料；审查时 adb 无设备且无 AVD |

## 1. 目录刷新与任务状态

页面同步系统已安装版本时，不再清空活动下载或覆盖安装阶段。目录升级可用性的刷新通过状态中心原子更新，只调整可用性，保留执行中的升级和需要重试的失败。执行者仍负责产生任务终态。

验证：`:business:testDebugUnitTest :business:testReleaseUnitTest`，两变体各 14 个测试类、118 项，0 失败、0 错误、0 跳过。

## 2. APK 失效与重新下载

安装失败只有在下载完成且仍有产物引用时才提供重试安装；产物失效则提供重新下载。APK 校验失败先将持久化下载任务标记失败、清空旧分片及续传标识，再回收 APK。重新下载受理后清除旧安装失败阶段，恢复暂停、继续及安装动作。

验证：business Debug / Release 各 120 项通过，0 失败、0 错误。
