# 2026-09 客户端审查修复记录

本轮基于 `dd947238` 审查后的问题清单，按项修复、验证并提交。产品定位遵循 [产品定位与平台边界](00-产品定位与平台边界.md)。本记录中的测试是 JVM / Robolectric 验证，不代表真机或生产服务验收。

| 顺序 | 项目 | 状态与验证 |
| --- | --- | --- |
| 1 | 页面刷新保留活动下载、安装、升级状态及失败原因 | 已完成。business Debug / Release 各 118 项测试通过；新增刷新活动阶段与失败状态回归。 |
| 2 | APK 失效后的重新下载与持久化任务一致性 | 已完成。business 两变体各 120 项通过；覆盖失效记录、缺失产物和重新下载后的暂停/恢复/安装。 |
| 3 | 安装异常业务边界及取消传播 | 已完成。全项目 Debug 329 / Release 331 项通过；覆盖目录异常、安装器异常、镜像失败和取消。 |
| 4 | 外部卸载对账与打开失败反馈 | 已完成。Debug 336 / Release 338 项通过；覆盖权威缺失、不可见包、目录缺项、活动安装及打开失败。 |
| 5 | 清理安装失败后恢复可操作的 APK 状态 | 已完成。business 两变体各 131 项通过；有效 APK 可安装、可清理，活动会话及已安装版本受保护。 |
| 6 | 安装成功后清理失败的缓存可追踪与重试 | 已完成。business 两变体各 133 项通过；删除失败保留任务与统计，恢复后重试成功。 |
| 7 | CI Release 单测、Lint、报告与文档基线 | 已完成。全门禁通过：Debug 340 / Release 342 项；两变体 Lint 各 0 错误、6 条警告；Debug / 未签名 Release APK 构建通过。 |
| 8 | 搜索关键词及分类的进程恢复 | 已完成。app / feature-search 两变体测试通过；验证 SavedState 的 Parcel 往返及全新 ViewModel 恢复。 |
| 9 | 运行时短期鉴权边界 | 客户端及接入文档已完成。Debug 350 / Release 352 项通过；临时签名构建、模式正向校验及固定凭证拒绝通过；后端签发/刷新待资料。 |
| 10 | Lint 可执行改善 | 已完成。Debug 356 / Release 358 项通过，两变体 Lint 各 0 错误、1 条合法布局提示；Debug / 未签名 Release APK 构建通过。 |
| 11 | 真机与生产验收 | NOT RUN。设备与服务条件仍缺失；已复核采集入口，补齐回归步骤和未执行记录，不能视为验收完成。 |

## 1. 目录刷新与任务状态

页面同步系统已安装版本时，不再清空活动下载或覆盖安装阶段。目录升级可用性的刷新通过状态中心原子更新，只调整可用性，保留执行中的升级和需要重试的失败。执行者仍负责产生任务终态。

验证：`:business:testDebugUnitTest :business:testReleaseUnitTest`，两变体各 14 个测试类、118 项，0 失败、0 错误、0 跳过。

## 2. APK 失效与重新下载

安装失败只有在下载完成且仍有产物引用时才提供重试安装；产物失效则提供重新下载。APK 校验失败先将持久化下载任务标记失败、清空旧分片及续传标识，再回收 APK。重新下载受理后清除旧安装失败阶段，恢复暂停、继续及安装动作。

验证：business Debug / Release 各 120 项通过，0 失败、0 错误。

## 3. 安装和主动作异常边界

安装入口将目录读取、文件和持久化异常转为可重试的业务失败，保留有效 APK。主动作执行器统一反馈异常并继续传播协程取消；安装失败后不再继续读取升级信息。系统成功事件先确认安装版本，后续镜像写入失败只记诊断，不推翻系统事实。六个页面均接入共享状态反馈。

验证：全项目 Debug 329 项、Release 331 项通过，0 失败、0 错误。新增异常测试初次运行暴露了 JVM 测试替身没有覆盖 Android 日志方法的问题，补齐日志替身后完整门禁通过。

## 4. 系统安装与卸载事实

Repository 显式返回已安装列表和系统确认不存在的 appId。目录与镜像只提供包名映射；Android 11+ 缺少完整包可见性时不推断卸载。状态中心应用卸载事实前检查查询期间的状态变化及活动安装会话，保留下载与任务状态。壳层初始化和回到前台时刷新系统事实；打开失败会反馈原因并刷新，成功重试会清除旧反馈。

验证：全项目 Debug 336 / Release 338 项通过；最终业务层和数据层改动另行执行两变体测试通过。真实系统卸载与前台切换仍需设备验收。

## 5. 清理失败记录与安装就绪

清理安装失败只复位失败阶段，并在产物锁内执行。有效 APK 保持下载完成，安装状态恢复为非活动状态；真实 WAITING 安装阶段禁止重复主动作。无效 APK 的文件、分片和任务记录一并回收。保留已有安装版本，不把升级失败清理误当成卸载。

验证：business 两变体各 131 项通过。跨安装/下载管理器回归确认：清理失败后，完成任务清理返回 1，APK 与任务记录均删除。

## 6. 安装后的产物回收

安装成功后先删除 APK，再删除下载任务记录。文件删除失败时保留完成任务、路径和清理反馈，缓存统计仍能发现文件；下载管理器恢复后可再次清理。任务元数据删除失败也不推翻系统安装成功事实，不恢复已经删除的 APK 引用。

验证：business 两变体各 133 项通过。新增跨管理器回归验证未删除的 1024 字节 APK 在恢复后仍被统计，重试后文件和任务删除、统计归零，已安装版本保留。

## 7. 两种变体的 CI 与报告

CI 增加 Release 单测与 Lint，保留生产配置和临时签名构建检查，失败时也尝试归档完整报告。标准库 Python 汇总器读取 JUnit / Lint XML，报告缺失或检查失败返回非零，并生成带提交 SHA 的 JSON 与 Markdown。README 和接手文档改为指向本轮记录及 CI 产物，不继续维护陈旧测试类数量表。

验证：汇总器 3 项正反向测试通过，YAML 解析通过；本地全门禁退出 0（4m 52s）。Debug 340 / Release 342 项，0 失败、0 错误、0 跳过；每变体 13 份 Lint 报告，0 错误、6 条警告。Debug / 未签名 Release 构建通过，不代表生产签名或服务验收。

## 8. 搜索输入和筛选恢复

搜索关键词与分类在变化时立即写入 SavedStateHandle，早于查询防抖。Fragment 的手动工厂使用 CreationExtras 创建并恢复 Handle，延迟服务初始化也能读到恢复后的输入。页面类型继续由 Fragment arguments 保存。

验证：app / feature-search 的 Debug、Release 单测通过。新增用例将 Activity 状态经 Parcel 序列化后创建新的 Activity、Fragment 和 ViewModel，确认关键词、分类与 EditText 内容都恢复；该用例是 Robolectric 状态恢复验证，非设备进程杀死验收。

## 9. 运行时短期鉴权

BuildConfig 移除固定认证值，生产门禁拒绝旧固定认证配置。目录与 CDN 分别使用内存中的短期 Bearer 凭证，绑定 HTTPS origin、最长 15 分钟、提前 30 秒停止使用，并以单调时钟限制存续时间。每次 HTTP 请求读取最新值；目录错误诊断只记录状态码，避免响应正文回显凭证。发布配置、模板和相关验收文档已同步；具体边界和剩余服务接入见 [运行时短期鉴权接入](35-运行时短期鉴权接入.md)。

验证：全项目 Debug 350 / Release 352 项通过。新增凭证边界及 HTTP loopback 用例覆盖更新、过期、清除、时钟回拨、跨 origin、注入与错误正文。两个生成的 BuildConfig 均无固定鉴权字段。临时证书的 `assembleProductionRelease` 与 apksigner 校验通过；两种模式配置正向通过，旧固定认证值按预期被拒绝。临时签名仅为本地工程验证；无后端接口资料，登录、签发、刷新和生产服务联调未执行。

## 10. Lint 与平台存储适配

目录排名改为资源格式化，按当前 Locale 显示数字；系统设置 URI 使用现有 KTX。下载器必须显式注入目录空间查询，由 AndroidStorageInfoProvider 同时服务策略预检、分片目录和最终 APK 目录。策略预检取系统可分配量与实际可写量的较小值，系统查询失败退回实际可写量；下载每 MiB 的高频复查只读文件系统，避免反复跨进程查询。无效目录返回零。未调用 allocateBytes，不把尚未回收的系统缓存算作已经可写的空间。

桌面 `sw900dp-land` 使用侧栏，未声明手机和平板的 `bottomNav`；MainActivity 使用可空访问，现有 ShellCompatibilityTest 覆盖桌面侧栏。保留这一合法布局差异产生的 InconsistentLayout 提示，不增加占位控件或关闭检查。

验证：完整门禁退出 0（5m 9s），Debug 63 个测试类 / 356 项、Release 64 个测试类 / 358 项，0 失败、0 错误、0 跳过。存储适配器新增 6 项回归，包含高频检查不调用系统存储服务的约束。两变体各 13 份 Lint 报告，均为 0 错误、1 条上述布局提示；Debug / 未签名 Release APK 构建通过。

## 11. 设备与生产验收条件

前 10 项客户端修复已按项提交并推送，代码修复提交为 `375df89c6fce4f36cb1a414797b76123ba31a555`。本节和验收清单是执行准备与阻塞记录，不能替代真实设备或生产服务结果。

2026-09-12 收尾复查：

| 检查 | 实际结果 |
| --- | --- |
| `adb devices -l` | 命令成功，设备列表为空 |
| `avdmanager list avd` | 命令成功，没有已创建的虚拟设备 |
| `collect-android-baseline.ps1` | 明确拒绝采集：`Exactly one authorized Android device is required; found 0.`；没有产生虚构设备报告 |
| 真实目录/CDN、认证会话和测试 APK | 尚未提供可联调的服务契约和配置，登录、签发、刷新、真实 TLS 与下载升级链路未执行 |
| 设备与生产验收结论 | **NOT RUN**；JVM / Robolectric、loopback 和临时签名构建仅支持各自本地工程结论 |

[设备回归清单](25-真机回归测试清单.md) 新增 R01–R10，覆盖活动任务刷新、APK 失效、下架异常、系统卸载、缓存清理、搜索进程恢复、API 35/36 UI、后台与通知限制、低空间及真实认证续传。44 行原记录模板均初始化为 NOT RUN，新增项也保持未执行；[联调清单](30-验收与联调清单.md) 和 [发布基线](33-生产发布配置与设备验证基线.md) 已同步。

继续执行需要：可授权连接的 Android 测试设备及目标版本/窗口矩阵；后端仓库或登录/签发/刷新接口文档、测试目录/CDN 地址和可校验身份的测试 APK。令牌、签名私钥和密码经受控环境提供，不进入文档或提交。设备就绪后先运行现有基线脚本，再执行 docs/25 与 [短期鉴权接入](35-运行时短期鉴权接入.md) 的对应场景，并为每个 PASS/FAIL 附上实际证据。

本轮本地原始证据保存在仓库外 `D:\workspace\.codex-audits\CarAppStore\20260912-dd94723`：最终门禁日志为 `fix-10-final.log`，设备条件为 `fix-11-adb.log`、`fix-11-avd.log`、`fix-11-device-blocker.log`。CI 自动生成的 `android-checks.json` / `.md` 随对应提交的验证产物归档，报告中的提交 SHA 用于区分不同轮次。
