# 25. Android 设备矩阵回归测试清单

> 最后更新：2026-09-12
> 本文档面向 QA 和开发人员，用于在手机、平板、桌面级大屏及其他 Android 真实设备上逐项执行回归验证。车载 OEM 项为可选专项，不属于通用版本门禁。
> 生产配置和证据命名规则见 `docs/33-生产发布配置与设备验证基线.md`；没有设备报告、日志和构建摘要时不得把手工项标记为 PASS。

## 1. 测试前置条件

### 1.1 设备信息记录

每轮测试开始前，先填写以下模板，确保结果可追溯。

```
| 字段         | 值                            |
|-------------|-------------------------------|
| 设备类型     | 手机 / 平板 / 大屏 / 可选车载 |
| 设备型号     |                               |
| 车型（可选） | 仅车载专项填写                |
| ROM 版本     |                               |
| Android 版本 |                               |
| 系统构建号   |                               |
| 屏幕分辨率   |                               |
| 网络条件     | Wi-Fi / 4G / 以太网 / 无网络  |
| 测试日期     |                               |
| 测试人员     |                               |
| 构建版本号   |                               |
```

### 1.2 测试环境确认

最低设备覆盖：

| 设备组 | 最低覆盖 | 目的 |
|---|---|---|
| 紧凑窗口手机 | API 26-28、API 30-34、API 35-36 各至少 1 台或 1 个可追溯环境 | 覆盖最低版本、主流存量和当前系统行为 |
| 中等窗口/平板 | `sw600dp`，至少 1 台真实平板 | 验证双列布局、权限确认页和旋转/多窗口 |
| 扩展窗口/桌面级大屏 | `sw900dp-land`，至少 1 个 Android 大屏或可追溯窗口环境 | 验证侧栏、任务摘要和横屏交互 |
| 可选车载发行目标 | 目标 ROM 与 OEM 协议设备 | 仅车载发行执行，其他发行标记 N/A |

在开始测试前逐项确认：

- [ ] APK 已通过 `assembleProductionRelease` 构建、签名验证并安装到设备
- [ ] 应用可正常冷启动，无闪退
- [ ] Release 构建固定使用 PROD，目录/CDN/鉴权与本轮发布配置一致
- [ ] 目录源可正常加载（网络连通或资源目录回退正常）
- [ ] 非 LOCAL_SIM 目录已为每个 APK 提供正数 `versionCode` 和签名证书 SHA-256 摘要
- [ ] 设备存储空间充足（>100MB 可用）
- [ ] 日志工具已就绪（adb logcat；车载专项可补充车机调试口）
- [ ] 网络抓包工具已就绪（如需验证 HTTP 请求）

建议先执行以下命令生成只含设备事实和 APK 摘要的基线报告，再把本页的手工结果附到同一轮证据目录：

```powershell
.\tools\device-validation\collect-android-baseline.ps1 `
  -ApkPath .\app\build\outputs\apk\release\app-release.apk
```

## 2. 下载链路回归

### 2.1 基本下载

- [ ] **正常下载完成（小文件 <10MB）**：点击下载后进度条平滑推进，状态从"下载中"变为"下载完成"，文件完整
- [ ] **正常下载完成（大文件 >100MB）**：长时间下载过程中进度条持续更新，不出现 ANR 或卡死，下载完成通知正常

### 2.2 下载控制

- [ ] **下载暂停并恢复**：暂停后进度条停止，恢复后从断点继续，进度值衔接无回退
- [ ] **下载取消并确认清理**：取消后任务从列表移除，临时文件和元数据被清理，存储空间恢复

### 2.3 网络异常

- [ ] **网络断开后下载失败并自动标记**：断网后任务状态变为"下载失败"，错误信息可读
- [ ] **网络恢复后重试下载**：重新点击下载后从断点续传，不从头开始
- [ ] **冷启动后恢复上次下载进度**：下载中途强杀进程，重新打开后任务仍存在且进度正确，可继续下载

### 2.4 下载完整性

- [ ] **Range 分段下载正确合并**：大文件分段下载后合并结果与源文件一致，安装包可正常解析
- [ ] **下载文件校验（SHA-256 匹配）**：校验通过时正常进入安装流程
- [ ] **下载文件校验（SHA-256 不匹配）**：校验失败时标记错误，提示用户重试，不进入安装

### 2.5 边界条件

- [ ] **存储空间不足时拒绝下载**：可用空间低于 APK 大小时拒绝发起下载，提示"存储空间不足"
- [ ] **同一应用重复点击下载**：不产生重复下载任务，已有任务继续执行
- [ ] **非法目录标识回退**：目录包含路径穿越、非法包名或重复标识时，整份来源被拒绝并回退，不创建越界任务目录

## 3. 安装链路回归

### 3.1 正常安装

- [ ] **正常安装完成**：下载完成后自动或手动触发安装，安装进度正常，最终显示"安装成功"
- [ ] **系统确认页弹出并用户确认**：PackageInstaller 确认页正常弹出，点击确认后安装继续
- [ ] **安装成功后产物回收**：APK 删除成功后清理下载任务；删除失败时保持安装成功事实，缓存统计和清理入口仍能发现产物并重试
- [ ] **安装前 APK 身份校验通过**：包名、`versionCode` 和签名摘要与目录一致后才创建系统 Session
- [ ] **安装后事实核对通过**：成功回调后 PackageManager 查询到的实际包名和版本一致，页面才显示“安装成功”

### 3.2 安装中断

- [ ] **用户拒绝安装确认**：系统确认页点击取消，应用状态回退到"可安装"，可重新触发
- [ ] **APK 损坏时引导重新下载**：错误信息明确，主按钮提供重新下载；只有有效 APK 的系统安装失败才提供重试安装
- [ ] **APK 包名/版本/签名不匹配被拦截**：不弹系统安装页、不创建 Session，下载产物被标记为不可复用
- [ ] **成功回调后系统事实不一致被拦截**：记录事实校验失败，不误报安装成功
- [ ] **安装中断后恢复（会话持久化）**：安装过程中强杀进程，重新打开后安装会话可恢复或重新发起

### 3.3 升级安装

- [ ] **升级安装（覆盖已安装应用）**：已安装应用有新版本时，覆盖安装正常完成，数据不丢失
- [ ] **降级安装被拦截**：尝试安装低版本时系统拒绝，客户端状态正确回退

## 4. 升级链路回归

### 4.1 升级检测

- [ ] **检测单个应用有升级**：已安装应用有新版本时，详情页或"我的应用"中显示升级标识
- [ ] **检测全部应用升级**：调用 checkAllUpgrades 后，所有有升级的应用均被正确标记
- [ ] **版本号相同时不触发升级**：已安装版本与最新版本相同时，hasUpgrade 为 false

### 4.2 升级执行

- [ ] **单个应用升级成功**：点击升级后，下载-校验-安装全链路顺利完成
- [ ] **批量串行升级成功**：选择多个应用批量升级，依次串行执行，全部完成
- [ ] **批量升级中某个失败时停止后续**：队列中某个应用升级失败，后续应用不再执行，已完成的保持成功状态
- [ ] **升级失败后可重试**：失败的应用可单独或重新批量触发升级

## 5. 策略拦截回归

### 5.1 网络策略

- [ ] **非 Wi-Fi 时下载被拦截**：断开 Wi-Fi 仅保留移动网络时，发起下载弹出拦截提示
- [ ] **Wi-Fi 恢复后拦截自动解除**：重新连接 Wi-Fi 后，可正常发起下载

### 5.2 存储策略

- [ ] **存储空间不足时安装被拦截**：可用空间不足时，安装操作被拦截并提示

### 5.3 可选车载策略

- [ ] **行车状态下安装被拦截（车载发行目标）**：启用 OEM 信号后，行车状态时安装操作被拦截，提示“行车中无法安装”
- [ ] **驻车后策略自动刷新（车载发行目标）**：从行车切换到驻车后，被拦截的操作可正常发起
- [ ] **非车载目标不依赖车况信号**：未启用车载能力时，缺少 OEM 信号不会阻断通用安装/升级流程

### 5.4 策略提示

- [ ] **策略拦截后错误信息正确展示**：拦截原因文案清晰（如“当前非 Wi-Fi 网络”“存储空间不足”；车载专项可含“行车中暂不可操作”），并提供可操作的后续引导

## 6. 状态与页面回归

### 6.1 首页与搜索

- [ ] **首页应用列表加载**：应用列表正常展示，包含应用图标、名称、标签、简介
- [ ] **搜索功能正常**：输入关键词后搜索结果匹配，结果列表展示完整
- [ ] **搜索关键词高亮或匹配验证**：搜索结果与输入关键词相关

### 6.2 详情页

- [ ] **详情页展示正确**：应用详情页包含完整信息（描述、版本、大小、权限、评分、开发者）
- [ ] **详情页主操作按钮状态正确**：未安装显示"下载"、下载中显示"暂停"、下载完成显示"安装"、已安装显示"打开"、有升级显示"升级"

### 6.3 我的应用

- [ ] **我的应用列表展示**：已安装应用正确列出，与系统 PackageManager 一致
- [ ] **我的应用升级标识**：有升级的应用显示升级角标

### 6.4 任务中心

- [ ] **下载中心任务列表**：下载中、已暂停、已完成的任务均正确展示
- [ ] **安装中心任务列表**：安装中、等待确认、已完成、已失败的任务均正确展示
- [ ] **升级中心任务列表**：待升级、升级中、已完成、已失败的升级任务均正确展示

### 6.5 实时状态

- [ ] **下载过程中进度条实时更新**：进度百分比平滑递增，与实际下载字节对应
- [ ] **安装确认等待状态正确展示**：系统确认页弹出期间，任务状态显示"等待用户确认"
- [ ] **错误信息可读且可操作**：错误状态下展示错误原因，并提供"重试"或"取消"操作

## 7. 异常场景回归

### 7.1 进程级异常

- [ ] **强杀进程后冷启动状态恢复**：下载中/安装中/升级中的任务，强杀后重新打开状态不丢失
- [ ] **长时间后台运行后回到前台状态正确**：应用切到后台运行超过 30 分钟后切回前台，任务列表和状态正确

### 7.2 操作级异常

- [ ] **快速连续点击不产生重复任务**：对同一应用快速点击下载/安装按钮 5 次以上，仅产生一个任务
- [ ] **同一应用同时下载和安装不冲突**：极端情况下不会同时出现下载和安装两个任务

### 7.3 系统级异常

- [ ] **低内存场景下应用不崩溃**：通过开发者选项限制后台进程后，应用主流程不崩溃
- [ ] **存储空间在下载过程中耗尽**：下载过程中可用空间降为 0，任务标记失败并提示，应用不崩溃
- [ ] **系统时间被篡改**：设备时间设置到过去或未来，不影响本地缓存的正常读取

## 8. 记录模板

### 8.1 测试结果记录

这是空白记录模板，默认 `NOT RUN`。填写真实设备与证据后才能改为 `PASS` 或 `FAIL`；仅不适用的发行目标填写 `N/A` 并说明原因。

```
| 编号 | 测试项                       | 设备信息            | 结果  | 备注               |
|------|-----------------------------|--------------------|-------|--------------------|
| D01  | 下载-正常完成（小文件）       | 待填 | NOT RUN |                    |
| D02  | 下载-正常完成（大文件）       | 待填 | NOT RUN |                    |
| D03  | 下载-暂停并恢复              | 待填 | NOT RUN |                    |
| D04  | 下载-取消并清理              | 待填 | NOT RUN |                    |
| D05  | 下载-断网失败标记            | 待填 | NOT RUN |                    |
| D06  | 下载-恢复后断点续传          | 待填 | NOT RUN |                    |
| D07  | 下载-冷启动进度恢复          | 待填 | NOT RUN |                    |
| D08  | 下载-Range 合并正确          | 待填 | NOT RUN |                    |
| D09  | 下载-SHA-256 校验通过        | 待填 | NOT RUN |                    |
| D10  | 下载-SHA-256 校验失败        | 待填 | NOT RUN |                    |
| D11  | 下载-存储不足拒绝            | 待填 | NOT RUN |                    |
| I01  | 安装-正常完成                | 待填 | NOT RUN |                    |
| I02  | 安装-系统确认页弹出          | 待填 | NOT RUN |                    |
| I03  | 安装-用户拒绝确认            | 待填 | NOT RUN |                    |
| I04  | 安装-APK 损坏失败回退        | 待填 | NOT RUN |                    |
| I05  | 安装-中断后会话恢复          | 待填 | NOT RUN |                    |
| I06  | 安装-成功后任务清理          | 待填 | NOT RUN |                    |
| I07  | 安装-升级覆盖安装            | 待填 | NOT RUN |                    |
| U01  | 升级-检测单个应用升级        | 待填 | NOT RUN |                    |
| U02  | 升级-检测全部应用升级        | 待填 | NOT RUN |                    |
| U03  | 升级-单个应用升级成功        | 待填 | NOT RUN |                    |
| U04  | 升级-批量串行升级成功        | 待填 | NOT RUN |                    |
| U05  | 升级-批量中某个失败停止后续  | 待填 | NOT RUN |                    |
| U06  | 升级-失败后可重试            | 待填 | NOT RUN |                    |
| U07  | 升级-版本相同不触发          | 待填 | NOT RUN |                    |
| P01  | 策略-非 Wi-Fi 拦截下载       | 待填 | NOT RUN |                    |
| P02  | 策略-存储不足拦截安装        | 待填 | NOT RUN |                    |
| P03  | 可选车载策略-行车状态拦截安装 | 待填 | NOT RUN | 仅车载发行目标执行 |
| P04  | 策略-拦截提示文案正确        | 待填 | NOT RUN |                    |
| S01  | 页面-首页列表加载            | 待填 | NOT RUN |                    |
| S02  | 页面-搜索功能                | 待填 | NOT RUN |                    |
| S03  | 页面-详情页展示              | 待填 | NOT RUN |                    |
| S04  | 页面-我的应用列表            | 待填 | NOT RUN |                    |
| S05  | 页面-下载中心列表            | 待填 | NOT RUN |                    |
| S06  | 页面-安装中心列表            | 待填 | NOT RUN |                    |
| S07  | 页面-升级中心列表            | 待填 | NOT RUN |                    |
| S08  | 页面-下载进度实时更新        | 待填 | NOT RUN |                    |
| S09  | 页面-安装等待确认状态        | 待填 | NOT RUN |                    |
| S10  | 页面-错误信息可读可操作      | 待填 | NOT RUN |                    |
| E01  | 异常-强杀后冷启动恢复        | 待填 | NOT RUN |                    |
| E02  | 异常-连续点击无重复任务      | 待填 | NOT RUN |                    |
| E03  | 异常-同时下载安装不冲突      | 待填 | NOT RUN |                    |
| E04  | 异常-长时间后台回前台        | 待填 | NOT RUN |                    |
| E05  | 异常-低内存不崩溃            | 待填 | NOT RUN |                    |
```

### 8.2 缺陷记录模板

```
| 缺陷编号 | 关联测试项 | 复现步骤                     | 期望行为           | 实际行为 | 严重程度 | 日志片段 |
|----------|-----------|------------------------------|--------------------|----------|---------|----------|
| BUG-001  | D01       | 1. 打开应用 2. 点击下载 xxx | 正常下载完成       | 闪退     | P0      | ...      |
```

### 8.3 结果统计汇总

总数按本轮实际执行清单填写，包括下节新增回归；`NOT RUN` 不得计为通过，缺少设备时不填写通过率。

```
| 分类         | 总数 | PASS | FAIL | NOT RUN | N/A | 通过率 |
|-------------|------|------|------|---------|-----|-------|
| 下载链路     |      |      |      |         |     |       |
| 安装链路     |      |      |      |         |     |       |
| 升级链路     |      |      |      |         |     |       |
| 策略拦截     |      |      |      |         |     |       |
| 状态与页面   |      |      |      |         |     |       |
| 异常场景     |      |      |      |         |     |       |
| 本轮新增回归 |      |      |      |         |     |       |
| **合计**     |      |      |      |         |     |       |
```

## 9. 2026-09 审查修复的设备回归

本轮执行条件与结果见 [客户端审查修复记录](34-2026-09客户端审查修复记录.md)。以下条目均尚未执行；现有 JVM / Robolectric 用例不能替代设备证据。为每个结果补充设备基线、构建 SHA、APK 摘要、环境和日志/截图位置。

| 编号 | 操作与预期 | 本轮结果 |
| --- | --- | --- |
| R01 | 分别在升级下载中、等待安装确认和安装中刷新目录/切换页面，进度、阶段和禁用动作保持正确，任务不会重复创建 | NOT RUN |
| R02 | 下载后让 APK 缺失或身份校验失败，主按钮提供重新下载；重新下载后的暂停、继续和安装均可用 | NOT RUN |
| R03 | 下载完成后目录下架该应用，再点击安装，错误进入可恢复的业务状态；有效 APK 和已成功安装的系统事实不会被异常覆盖 | NOT RUN |
| R04 | 在系统设置卸载已安装应用，再返回商店，权威可见性允许时恢复下载动作；打开失败有反馈，活动安装会话仍受保护 | NOT RUN |
| R05 | 拒绝系统安装后清理失败记录，有效 APK 仍可安装，也可从下载中心删除；系统安装成功但文件删除失败时，缓存仍可统计和重试清理 | NOT RUN |
| R06 | 输入搜索词并选择分类，退到后台后通过系统进程回收再恢复原任务，输入和筛选保留；与强行停止、清除数据分别记录，不能互作替代证据 | NOT RUN |
| R07 | API 35/36 检查边到边、系统栏、IME、横竖屏、暗色和 2 倍字体；分别记录手机、真实平板及扩展窗口的按钮可见性、侧栏滚动和截图 | NOT RUN |
| R08 | 拒绝通知权限、切后台、断网并恢复，验证前台服务限制、失败反馈、暂停/续传和冷启动任务恢复；补充长时间下载是否出现 ANR | NOT RUN |
| R09 | 使用受控低空间测试环境验证分片、合并和安装预算；可回收缓存不应被当成已可写空间，空间耗尽有可恢复的失败反馈 | NOT RUN |
| R10 | 按 [短期鉴权接入](35-运行时短期鉴权接入.md) 对真实 TLS 目录/CDN 验证签发、刷新、401/403、登出、冷启动、Range 续传和签名 URL 过期，使用可校验身份的真实 APK 完成升级 | NOT RUN |
